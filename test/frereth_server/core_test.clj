(ns frereth-server.core-test
  (:use clojure.test
        ;expectations
        frereth-server.core))

(deftest a-test
  (testing "Verify basic testing framework"
    (is (= 1 1))))

;;(expect nil? nil)

