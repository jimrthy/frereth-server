(ns com.frereth.server.getty
  "Think of the getty process that gets forked near the end of linux's init

It's the part that sets up the terminal connections for logging in.

Except that it's just another process, which really means it's just an App/Plugin.

No reason to do anything special here, either")
